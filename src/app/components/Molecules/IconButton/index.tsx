// Libraries
import React, { memo } from 'react';

// Styled
import { IconButtonWrapper } from './styled';

interface IconButtonProps {}

export const IconButton: React.FC<IconButtonProps> = memo(props => {
  return <IconButtonWrapper></IconButtonWrapper>;
});
