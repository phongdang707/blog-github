// Libraries
import React, { memo } from 'react';

// Styled
import { ButtonIconWrapper } from './styled';

interface ButtonIconProps {}

export const ButtonIcon: React.FC<ButtonIconProps> = memo(props => {
  return <ButtonIconWrapper></ButtonIconWrapper>;
});
